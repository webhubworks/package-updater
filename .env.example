# Directory containing your local repos. The tool walks this directory
# recursively (skipping vendor/node_modules/.git) for repos that depend
# on the chosen composer package. Grouping folders like
# `~/reps/my7steps/<repo>` are supported.
REPOS_DIR="${HOME}/reps"
